spring:
  datasource:
    url: "jdbc:h2:mem:myDb"
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
logging:
  level:
    github.skreutz: trace
    #root: DEBUG
